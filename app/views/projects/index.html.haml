.search-project-body
  .container
    .sidebar
      = render 'projects/sidebar'
    .page-main
      %section.Grid.amount
        = render partial: 'toppage/partial/PcTopProjectList',
                  locals: { title: "検索結果 #{ @projects.count }件",
                            projects: @projects }

