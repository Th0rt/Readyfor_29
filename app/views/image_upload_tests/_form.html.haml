= form_with(model: image_upload_test, local: true) do |form|
  - if image_upload_test.errors.any?
    #error-explanation
      %h2
        = pluralize(image_upload_test.errors.count, "error")
        prohibited this image_upload_test from being saved:
      %ul
        - image_upload_test.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label :image
    = form.file_field :image
  .actions
    = form.submit
