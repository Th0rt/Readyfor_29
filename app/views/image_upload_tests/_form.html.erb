<%= form_with(model: image_upload_test, local: true) do |form| %>
  <% if image_upload_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(image_upload_test.errors.count, "error") %> prohibited this image_upload_test from being saved:</h2>

      <ul>
      <% image_upload_test.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
