%h1 Toppage#index
%p Find me in app/views/toppage/index.html.erb
- unless user_signed_in?
  %button
    = link_to "ログイン画面/サインアップ画面へ", new_user_registration_path
- if user_signed_in?
  %button
    = link_to "ユーザー画面へ",user_path(id: current_user.id)
  %button
    = link_to "ログアウト", destroy_user_session_path, method: :delete
  = link_to project_path(1), class: "btn btn-default" do
    project_show
  = link_to new_project_path, class: "btn btn-default" do
    project_new
  = link_to edit_project_path(1), class: "btn btn-default" do
    project_edit
