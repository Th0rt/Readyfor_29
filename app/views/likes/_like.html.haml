%p.likes_count #{project.likes_count}人がお気に入りに追加しています
#like-buttons
  - if user_signed_in?
    - if project.like_user(current_user.id)
      .like-circle
        = button_to project_like_path(like, project_id: project.id), method: :delete, remote: true do
          .fa.fa-heart.fa-2x.heart-red
      %p.like-text お気に入り済み
    - else
      .like-circle
        = button_to project_likes_path(project), remote: true do
          .fa.fa-heart-o.fa-2x.heart-color
      %p.like-text お気に入りに追加する
  - else
    .like-circle
      = button_to new_user_session_path do
        .fa.fa-heart.fa-2x.heart-red
    %p.like-text お気に入りに追加する
