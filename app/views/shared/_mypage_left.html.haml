.mypage-top
  .mypage-image
    = link_to profile_user_path(current_user) do
      = image_tag("#{current_user.avatar}")
  .mypage-profile
    .mypage-name
      = link_to profile_user_path(current_user) do
        = current_user.nickname
    .mypage-edit
      = link_to "プロフィール編集", edit_user_path(id: current_user.id)
    .mypage-edit
      = link_to "トップページへ", root_path(id: 1)
